# Agent 工作约定（项目级模板）

本模板只写项目特有的规则。跨项目的个人默认，例如读够上下文、最小计划、授权边界、停下上报、
测试预算、状态词汇和 Skill 路由，放在全局 `~/.codex/AGENTS.md` / `~/.claude/CLAUDE.md`，
这里不重复。复制到项目后，把尖括号里的内容换成本项目的事实，用不上的段落直接删掉。

## 项目事实

- 目标与非目标：<一两句>
- 唯一事实源：<`.project-map/MAP.md` 或本项目已有的账本>。不另建 `REALITY.md`、第二套计划
  或并行状态文件；派生文档不得凌驾于代码、实测事实或这份事实源。
- 常用命令：<构建、测试、启动>
- 验收入口：<真实入口，以及哪类结论需要哪一层证据>

## 改动边界

1. 新增第二条生产路径时，必须能指出真实调用方/契约、故障模式、所有权、优先级和移除条件；
   否则停下来改小方案。
2. 安全、隐私、凭据、数据丢失、权限、迁移和外部副作用按本项目的真实威胁模型处理：
   <写明威胁模型，例如"默认操作者是自己机器上的合作者">。

## 测试预算

- 每个新增测试都要说明它验证哪条验收条件、没有它会漏掉哪个真实回归，以及为什么现有证据
  不足。测试数量和代码长度不是充分性的标准；需要更多覆盖时，按不同契约、边界或故障模式
  说明理由。
- <本项目不需要的测试形式，例如：没有证据时不新增矩阵、快照、端到端框架或测试基础设施。>

## 复审触发

- <本项目里属于评测面的目录，例如 `eval/`、`benchmark/`、`golden/`。改动它们时走
  `behavioral-acceptance-review`，实现者新增的测试只是待审证据，不是唯一 oracle。>

## 发布

- 提交、推送、发布、部署或合并只在获得明确授权时执行；普通推送失败时不改用强推。
